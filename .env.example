# Routine Minder Environment Variables
# Copy this file to .env and fill in your values

# API Configuration (Cloudflare Worker)
VITE_API_URL=https://routine-minder-api.your-subdomain.workers.dev
VITE_API_KEY=your-api-secret-key

# Google Sign-In (get from Google Cloud Console)
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable "Google Identity Services"
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add authorized origins: http://localhost:5173, https://your-domain.com
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Push Notifications (optional)
# Generate keys with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
